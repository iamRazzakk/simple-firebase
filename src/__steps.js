/**
 * visit.console.firebase.google.com
 * create project (skip google analytics)
 * register app (create config)
 * install firebase
 * add config file to my project
 * DANGER: Do not publish or make firebase config to public by pushing those to github.
 * export app from the firebase.config.js file: export default app
 * Login.jsx: import getAuth from firebase/auth
 * create auth
 * const auth = getAuth(app)
 * import google auth provider create a new provider 
 * use sing-in with popup and pass auth and provider
 * activate sing-in method (google,facebook,github)
 * */ 